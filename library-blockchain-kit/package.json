{
  "name": "library-blockchain-kit",
  "version": "1.0.0",
  "private": true,
  "description": "Blockchain-powered library management (borrow/return, deposits, reputation, QR-signed)",
  "scripts": {
    "postinstall": "hardhat",
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network localhost",
    "deploy:amoy": "hardhat run scripts/deploy.js --network amoy",
    "test": "hardhat test",
    "sign:qr": "node scripts/sign_qr.js"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.10"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2",
    "ethers": "^5.7.2",
    "qrcode": "^1.5.4"
  }
}